version: '3.8'

services:
  dynamodb:
    image: amazon/dynamodb-local
    ports:
      - "4566:8000"
    volumes:
      - "./dynamodb_data:/home/dynamodblocal/data"
    command: "-jar DynamoDBLocal.jar -sharedDb -dbPath /home/dynamodblocal/data"

#networks:
#  default:
#    external:
#      name: dynamoo-network

# Make sure to create the network
#   docker network create dynamoo-network
